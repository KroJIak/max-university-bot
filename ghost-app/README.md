# Ghost API

API для симуляции University API с использованием данных из базы данных вместо веб-скрапинга.

## 📋 Описание

**Ghost API** — это резервный API сервис, который симулирует работу University API, но использует данные из PostgreSQL базы данных вместо веб-скрапинга. Данные загружаются через CSV файлы через специальный эндпоинт `/upload`.

### Основные возможности

- 🔄 **Симуляция University API**: Те же эндпоинты, что и в university-app
- 📊 **Хранение данных**: Все данные хранятся в БД и возвращаются по запросам
- 📤 **Загрузка данных**: Прием CSV файлов с данными студентов, расписаний, преподавателей и т.д.
- 🏛️ **Multi-tenancy**: Поддержка множества университетов через заголовок `X-University-Id`
- 🔐 **Простая аутентификация**: Логин всегда успешен, если студент существует в БД

## 🎯 Использование

Ghost API активируется, если в админ-панели включен функционал, но не указан endpoint University API. В этом случае администратор:

1. Указывает значения вручную в админ-панели
2. Загружает CSV файл через эндпоинт `/upload`
3. Данные становятся доступными через стандартные эндпоинты

## 🏗️ Архитектура

```
ghost-app/
├── main.py              # Точка входа FastAPI приложения
├── api/                 # API эндпоинты
│   ├── students.py      # Эндпоинты для студентов
│   └── upload.py        # Эндпоинт для загрузки CSV
├── models/              # SQLAlchemy модели
│   ├── student.py       # Модель студента
│   ├── teacher.py       # Модель преподавателя
│   ├── schedule.py      # Модель расписания
│   ├── contact.py       # Модель контактов
│   └── platform.py      # Модель платформ
├── repositories/        # Репозитории для работы с БД
│   ├── student_repository.py
│   ├── teacher_repository.py
│   ├── schedule_repository.py
│   ├── contact_repository.py
│   └── platform_repository.py
├── services/            # Бизнес-логика
│   └── csv_parser.py    # Парсер CSV файлов
├── core/                # Ядро приложения
│   └── database.py      # Настройка БД
├── data/                # Статические данные
│   └── maps.json        # Данные о картах корпусов
├── requirements.txt     # Зависимости Python
└── Dockerfile          # Docker образ
```

## 📡 API Эндпоинты

Все эндпоинты требуют заголовок `X-University-Id` для идентификации университета.

### Студенты

#### `POST /students/login`
Логин студента (симуляция - всегда успешен, если студент существует).

**Запрос:**
```json
{
  "student_email": "student@university.edu",
  "password": "password"
}
```

**Ответ:**
```json
{
  "success": true,
  "error": null
}
```

#### `POST /students/personal_data`
Получить персональные данные студента.

**Запрос:**
```json
{
  "student_email": "student@university.edu"
}
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "full_name": "Иванов Иван Иванович",
    "group": "ИС-21",
    "course": "3",
    "photo": "https://example.com/photo.jpg"
  },
  "error": null
}
```

### Преподаватели

#### `POST /students/teachers`
Получить список всех преподавателей университета.

**Запрос:**
```json
{
  "student_email": "student@university.edu"
}
```

**Ответ:**
```json
{
  "success": true,
  "teachers": [
    {"id": "2173", "name": "Иванов Иван Иванович"},
    {"id": "2174", "name": "Петров Петр Петрович"}
  ],
  "error": null
}
```

#### `POST /students/teacher_info`
Получить информацию о конкретном преподавателе.

**Запрос:**
```json
{
  "student_email": "student@university.edu",
  "teacher_id": "2173"
}
```

**Ответ:**
```json
{
  "success": true,
  "departments": ["Кафедра информатики", "Кафедра математики"],
  "photo": "https://example.com/teacher.jpg",
  "error": null
}
```

### Расписание

#### `POST /students/schedule`
Получить расписание студента на неделю.

**Запрос:**
```json
{
  "student_email": "student@university.edu",
  "week": 1
}
```

**Параметры:**
- `week`: 1 = текущая неделя, 2 = следующая неделя

**Ответ:**
```json
{
  "success": true,
  "schedule": [
    {
      "date": "2024-01-15",
      "time_start": "09:00",
      "time_end": "10:30",
      "subject": "Математика",
      "type": "Лекция",
      "teacher": "Иванов И.И.",
      "room": "101"
    }
  ],
  "error": null
}
```

### Контакты

#### `POST /students/contacts`
Получить контакты деканатов и кафедр.

**Запрос:**
```json
{
  "student_email": "student@university.edu"
}
```

**Ответ:**
```json
{
  "success": true,
  "deans": [
    {
      "faculty": "Факультет информатики",
      "phone": "+7 (123) 456-78-90",
      "email": "dean@university.edu"
    }
  ],
  "departments": [
    {
      "faculty": "Факультет информатики",
      "department": "Кафедра информатики",
      "phones": "+7 (123) 456-78-91",
      "email": "dept@university.edu"
    }
  ],
  "error": null
}
```

### Платформы

#### `GET /students/platforms`
Получить список платформ университета.

**Ответ:**
```json
{
  "success": true,
  "platforms": [
    {
      "key": "moodle",
      "name": "Moodle",
      "url": "https://moodle.university.edu",
      "emoji": "📚"
    }
  ],
  "error": null
}
```

### Сервисы

#### `GET /students/services`
Получить список сервисов (статический список).

**Ответ:**
```json
{
  "success": true,
  "services": [
    {"key": "schedule", "name": "Расписание", "emoji": "📅"},
    {"key": "teachers", "name": "Преподаватели", "emoji": "👨‍🏫"},
    {"key": "map", "name": "Карта", "emoji": "🗺️"},
    {"key": "contacts", "name": "Контакты", "emoji": "📞"},
    {"key": "chats", "name": "Чаты", "emoji": "💬"}
  ],
  "error": null
}
```

### Карты

#### `GET /students/maps`
Получить список карт корпусов университета.

**Ответ:**
```json
{
  "buildings": [
    {
      "name": "Главный корпус",
      "latitude": 55.7558,
      "longitude": 37.6173,
      "yandex_map_url": "https://yandex.ru/maps/...",
      "gis2_map_url": "https://2gis.ru/...",
      "google_map_url": "https://maps.google.com/..."
    }
  ]
}
```

### Загрузка данных

#### `POST /upload`
Загрузить CSV файл с данными для университета.

**Параметры:**
- `university_id` (form data): ID университета
- `file` (file): CSV файл с данными

**Ответ:**
```json
{
  "success": true,
  "message": "Данные успешно импортированы",
  "imported": {
    "students": 100,
    "teachers": 50,
    "schedules": 500,
    "contacts": 20,
    "platforms": 5
  },
  "rows_count": 675,
  "error": null
}
```

## 🚀 Запуск

### Локальный запуск

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Настройте переменные окружения:**
   ```bash
   export GHOST_DATABASE_URL="postgresql://user:password@localhost:5432/ghost_db"
   export GHOST_API_HOST="0.0.0.0"
   export GHOST_API_PORT="8004"
   ```

3. **Запустите сервер:**
   ```bash
   uvicorn main:app --host 0.0.0.0 --port 8004
   ```

### Docker

1. **Соберите образ:**
   ```bash
   docker build -t ghost-api .
   ```

2. **Запустите контейнер:**
   ```bash
   docker run -d \
     -p 8004:8004 \
     -e GHOST_DATABASE_URL="postgresql://user:password@postgres:5432/ghost_db" \
     -e GHOST_API_HOST="0.0.0.0" \
     -e GHOST_API_PORT="8004" \
     --name ghost-api \
     ghost-api
   ```

### Docker Compose

Ghost API запускается автоматически при использовании основного `docker-compose.yml` проекта.

## ⚙️ Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `GHOST_DATABASE_URL` | URL подключения к PostgreSQL | `postgresql://maxbot:maxbot123@postgres:5432/ghost_db` |
| `GHOST_API_HOST` | Хост для запуска API | `0.0.0.0` |
| `GHOST_API_PORT` | Порт для запуска API | `8004` |

## 📊 Формат CSV файлов

CSV файлы должны содержать следующие колонки в зависимости от типа данных:

### Студенты
- `email` - Email студента
- `full_name` - Полное имя
- `group` - Группа
- `course` - Курс
- `photo` - URL фотографии (опционально)

### Преподаватели
- `teacher_id` - ID преподавателя
- `name` - Имя преподавателя
- `departments` - JSON массив кафедр (опционально)
- `photo` - URL фотографии (опционально)

### Расписание
- `student_email` - Email студента
- `date` - Дата (YYYY-MM-DD)
- `time_start` - Время начала (HH:MM)
- `time_end` - Время окончания (HH:MM)
- `subject` - Название предмета
- `type` - Тип занятия (Лекция, Практика и т.д.)
- `teacher` - Имя преподавателя
- `room` - Аудитория
- `week` - Номер недели (1 или 2)

### Контакты
- `type` - Тип контакта (`dean` или `department`)
- `faculty` - Факультет
- `department` - Кафедра (только для `department`)
- `phone` / `phones` - Телефон(ы)
- `email` - Email

### Платформы
- `key` - Ключ платформы
- `name` - Название платформы
- `url` - URL платформы
- `emoji` - Эмодзи для платформы

## 🔍 Health Check

#### `GET /health`
Проверка работоспособности API.

**Ответ:**
```json
{
  "status": "healthy"
}
```

#### `GET /`
Информация о сервисе.

**Ответ:**
```json
{
  "service": "Ghost API",
  "version": "1.0.0"
}
```

## 🗄️ База данных

Ghost API автоматически создает базу данных и таблицы при первом запуске, если они не существуют.

### Модели данных

- **Student**: Студенты университета
- **Teacher**: Преподаватели университета
- **Schedule**: Расписание занятий
- **Contact**: Контакты деканатов и кафедр
- **Platform**: Платформы университета

Все модели связаны с `university_id` для поддержки multi-tenancy.

## 🔧 Разработка

### Структура кода

- **API Layer** (`api/`): FastAPI роутеры и эндпоинты
- **Repository Layer** (`repositories/`): Доступ к данным через SQLAlchemy
- **Service Layer** (`services/`): Бизнес-логика (парсинг CSV)
- **Model Layer** (`models/`): SQLAlchemy модели
- **Core** (`core/`): Настройка БД и общие утилиты

### Добавление новых эндпоинтов

1. Создайте модель в `models/` (если нужна)
2. Создайте репозиторий в `repositories/`
3. Добавьте эндпоинт в `api/students.py` или создайте новый роутер
4. Подключите роутер в `main.py`

## 📝 Примечания

- Ghost API используется как резервный вариант, когда University API не настроен
- Все данные должны быть предварительно загружены через `/upload`
- Логин всегда успешен, если студент существует в БД (пароль не проверяется)
- Все эндпоинты требуют заголовок `X-University-Id` для multi-tenancy

## 🔗 Связанные сервисы

- [University API](../university-app/README.md) - Основной API с веб-скрапингом
- [MAX API](../app/README.md) - Основной API системы
- [Admin Panel](../admin-panel/README.md) - Админ-панель для управления

