# 🚀 Локальный запуск бота для разработки

Это руководство поможет вам запустить бота локально без использования Docker для более удобной разработки.

## ⚡ Быстрый запуск (одна команда)

**Просто скопируйте и вставьте в терминал:**

```bash
cd /home/goliluxa/workspace/Max-University-Bot/bot && go mod download && go mod tidy && go run main.go
```

**Убедитесь, что:**
- **Go 1.23 установлен** (если нет - см. раздел "Установка Go 1.23" ниже)
- У вас есть файл `.env` в корне проекта `/home/goliluxa/workspace/Max-University-Bot/.env` с переменной `MAX_BOT_TOKEN=ваш_токен`
- Docker контейнер бота остановлен (`docker stop maxbot_bot`)

### 🔧 Установка Go 1.23 (одна команда)

Если Go не установлен, выполните:

```bash
cd /tmp && wget https://go.dev/dl/go1.23.12.linux-arm64.tar.gz && sudo rm -rf /usr/local/go && sudo tar -C /usr/local -xzf go1.23.12.linux-arm64.tar.gz && echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc && source ~/.bashrc && go version
```

**Примечание:** Если версия `1.23.12` недоступна, проверьте последнюю доступную версию на https://go.dev/dl/ и замените номер версии в команде.

## 📋 Требования

- **Go 1.23** или выше
- **Git** (для клонирования/обновления кода)

## 🛠️ Установка Go 1.23 (если не установлен)

Если при запуске вы видите ошибку `Command 'go' not found`, установите Go 1.23:

### Вариант 1: Установка Go 1.23 вручную (рекомендуется)

```bash
# Скачиваем Go 1.23 для Linux ARM64 (Raspberry Pi)
# Проверяем последнюю доступную версию
LATEST_GO=$(curl -s https://go.dev/dl/ | grep -o 'go1\.23\.[0-9]*\.linux-arm64\.tar\.gz' | head -1)
cd /tmp
wget https://go.dev/dl/${LATEST_GO}

# Удаляем старую версию Go (если есть)
sudo rm -rf /usr/local/go

# Устанавливаем Go 1.23
sudo tar -C /usr/local -xzf ${LATEST_GO}

# Добавляем Go в PATH (добавьте это в ~/.bashrc или ~/.zshrc)
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
source ~/.bashrc

# Проверяем версию
go version
```

Или используйте конкретную версию (например, 1.23.12):

```bash
cd /tmp
wget https://go.dev/dl/go1.23.12.linux-arm64.tar.gz
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf go1.23.12.linux-arm64.tar.gz
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
source ~/.bashrc
go version
```

Должно показать: `go version go1.23.12 linux/arm64`

### Вариант 2: Через snap (но версия может быть другой)

```bash
sudo snap install go --classic
go version  # проверьте версию после установки
```

**Примечание:** Бот использует Go 1.23, как указано в `go.mod`. Рекомендуется установить именно эту версию.

## 🔧 Настройка окружения

### Файл `.env` в корне проекта

Бот автоматически подхватит файл `.env` из корня проекта:
- `/home/goliluxa/workspace/Max-University-Bot/.env`

Убедитесь, что в файле `.env` есть переменная:

```env
MAX_BOT_TOKEN=ваш_токен_бота_здесь
```

**Важно:** Не коммитьте файл `.env` в Git! Он уже должен быть в `.gitignore`.

Код конфигурации бота автоматически ищет `.env` в корне проекта, поэтому вам не нужно создавать отдельный файл в папке `bot/`.

## 🏃 Запуск бота (альтернативные варианты)

### Вариант 1: Прямой запуск через `go run`

Перед первым запуском установите зависимости:

```bash
cd /home/goliluxa/workspace/Max-University-Bot/bot
go mod download
go mod tidy
```

Затем запустите бота:

```bash
go run main.go
```

*(Или используйте команду из раздела "Быстрый запуск" выше - она включает установку зависимостей)*

### Вариант 2: Сборка и запуск бинарника

```bash
cd /home/goliluxa/workspace/Max-University-Bot/bot

# Сборка бинарника
go build -o bot main.go

# Запуск
./bot
```

### Вариант 3: Установка зависимостей и запуск

Если зависимости не установлены:

```bash
cd /home/goliluxa/workspace/Max-University-Bot/bot

# Установка зависимостей
go mod download

# Запуск
go run main.go
```

## 🛑 Остановка бота

Для остановки бота нажмите `Ctrl+C` в терминале, где он запущен.

## 🔍 Проверка работы

После запуска вы должны увидеть в консоли что-то вроде:

```
Bot started: ИмяБота (ID: 12345)
Bot is running. Press Ctrl+C to stop.
```

## 📝 Полезные команды для разработки

### Пересборка при изменениях (автоматический перезапуск)

Если хотите автоматически перезапускать бота при изменениях кода, можно использовать инструменты типа `air`:

```bash
# Установка air
go install github.com/cosmtrek/air@latest

# Создание конфига air
air init

# Запуск с автоперезапуском
air
```

### Установка зависимостей

```bash
go mod download
go mod tidy
```

### Проверка кода

```bash
go fmt ./...
go vet ./...
```

## ⚠️ Важные замечания

1. **Docker контейнер должен быть остановлен** перед локальным запуском, чтобы избежать конфликтов портов.
   
   Проверить запущенные контейнеры:
   ```bash
   docker ps | grep maxbot_bot
   ```
   
   Остановить контейнер:
   ```bash
   docker stop maxbot_bot
   ```

2. **Файл `.env`** должен находиться в корне проекта `/home/goliluxa/workspace/Max-University-Bot/.env` - бот автоматически его подхватит.

3. Если бот не запускается, проверьте:
   - Установлена ли Go версии 1.23+
   - Установлены ли все зависимости (`go mod download`)
   - Правильно ли указан `MAX_BOT_TOKEN` в `.env` (файл должен быть в корне проекта: `/home/goliluxa/workspace/Max-University-Bot/.env`)

## 🔄 Переключение между Docker и локальным запуском

### Для остановки Docker бота:
```bash
docker stop maxbot_bot
```

### Для запуска Docker бота обратно:
```bash
cd /home/goliluxa/workspace/Max-University-Bot
docker-compose -f docker-compose-arm64.yml up -d bot
```

## 📚 Дополнительная информация

- Модуль: `max-bot`
- Go версия: `1.23`
- Основные зависимости:
  - `github.com/joho/godotenv` - загрузка `.env` файлов
  - `github.com/max-messenger/max-bot-api-client-go` - клиент API Max Bot

---

**Приятной разработки! 🎉**

cd /home/goliluxa/workspace/Max-University-Bot/bot && go run main.go