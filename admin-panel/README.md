# Админ-панель университета

Flutter веб-приложение для управления университетом. Административный интерфейс для управления студентами, преподавателями, расписанием, модулями и другими аспектами университетской системы.

## Возможности

- 🔐 **Аутентификация** - безопасный вход в систему
- 👥 **Управление студентами** - добавление, редактирование и управление студентами
- 👨‍🏫 **Управление преподавателями** - управление информацией о преподавателях
- 📅 **Расписание** - создание и управление расписанием занятий
- 📚 **Управление модулями** - настройка учебных модулей
- 🗺️ **Карта** - интерактивная карта университета
- 📰 **Новости** - управление новостями университета
- ⚙️ **Настройки API** - конфигурация подключения к API

## Требования

- Flutter SDK ^3.9.2
- Dart SDK
- Node.js (для веб-сборки, опционально)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Max-University-Bot/admin-panel
```

2. Установите зависимости:
```bash
flutter pub get
```

3. Настройте конфигурацию API (если требуется):
```bash
cp config.js.template config.js
# Отредактируйте config.js с вашими настройками
```

## Запуск

### Локальная разработка

Для запуска в режиме разработки:

```bash
flutter run -d chrome
```

Или для веб-версии:
```bash
flutter run -d web-server --web-port=8080
```

### Сборка для production

Для сборки веб-версии:

```bash
flutter build web --release
```

Собранные файлы будут находиться в `build/web/`.

## Docker

Проект включает Dockerfile для контейнеризации приложения.

### Сборка Docker образа

```bash
# Используйте скрипт для оптимизированной сборки
./build.sh

# Или напрямую через Docker
DOCKER_BUILDKIT=1 docker build -t admin-panel .
```

### Запуск контейнера

```bash
docker run -p 8080:8080 \
  -e API_BASE_URL=https://your-api-url.com \
  admin-panel
```

### Оптимизация сборки

Для ускорения сборки используйте BuildKit:

```bash
export DOCKER_BUILDKIT=1
export COMPOSE_DOCKER_CLI_BUILD=1
./build.sh
```

Подробнее об оптимизации сборки см. [BUILD_OPTIMIZATION.md](./BUILD_OPTIMIZATION.md).

## Структура проекта

```
admin-panel/
├── lib/
│   ├── main.dart                 # Точка входа приложения
│   ├── models/                   # Модели данных
│   │   ├── student.dart
│   │   ├── teacher.dart
│   │   ├── schedule_event.dart
│   │   ├── module.dart
│   │   └── ...
│   ├── screens/                  # Экраны приложения
│   │   ├── login_screen.dart
│   │   ├── main_screen.dart
│   │   ├── students_screen.dart
│   │   ├── teachers_screen.dart
│   │   ├── schedule_screen.dart
│   │   └── ...
│   └── services/                 # Сервисы для работы с API
│       ├── api_service.dart
│       └── features_service.dart
├── web/                          # Веб-конфигурация
├── android/                      # Android конфигурация
├── ios/                          # iOS конфигурация
├── Dockerfile                    # Docker конфигурация
├── docker-entrypoint.sh          # Entrypoint скрипт для Docker
├── config.js.template            # Шаблон конфигурации
└── pubspec.yaml                  # Зависимости проекта
```

## Зависимости

Основные зависимости:

- `http` - для HTTP запросов
- `shared_preferences` - для локального хранения данных
- `file_picker` - для выбора файлов
- `csv` - для работы с CSV файлами
- `table_calendar` - календарь для расписания
- `flutter_map` / `google_maps_flutter_web` - карты
- `intl` - интернационализация

Полный список зависимостей см. в [pubspec.yaml](./pubspec.yaml).

## Конфигурация

### Переменные окружения

При запуске через Docker можно настроить следующие переменные:

- `API_BASE_URL` - базовый URL API сервера
- `API_TIMEOUT` - таймаут запросов (опционально)

### Конфигурационный файл

Для веб-версии используется `config.js`, который генерируется из шаблона `config.js.template` при запуске контейнера.

## Разработка

### Запуск тестов

```bash
flutter test
```

### Анализ кода

```bash
flutter analyze
```

### Форматирование кода

```bash
flutter format .
```

## Платформы

Приложение поддерживает:

- ✅ Web (основная платформа)
- ✅ Android
- ✅ iOS
- ✅ Linux
- ✅ macOS
- ✅ Windows

## Лицензия

[Укажите лицензию проекта]

## Поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.
