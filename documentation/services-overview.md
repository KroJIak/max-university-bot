# Обзор сервисов

Подробное описание всех микросервисов системы Max University Bot.

## 📋 Содержание

- [Архитектура системы](#архитектура-системы)
- [MAX API](#max-api)
- [Bot](#bot)
- [Admin Panel](#admin-panel)
- [University API](#university-api)
- [Ghost API](#ghost-api)
- [Mini App](#mini-app)
- [PostgreSQL](#postgresql)
- [Взаимодействие сервисов](#взаимодействие-сервисов)

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                      MAX Users                               │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
                ┌──────────────┐
                │     MAX      │
                │     Bot      │
                │   (bot/)     │
                └──────┬───────┘
                       │
                       ▼
                ┌──────────────┐
                │   MAX API    │
                │   (app/)     │◄──────┐
                └──────┬───────┘       │
                       │               │
        ┌──────────────┼───────────────┼──────────────┐
        │              │               │              │
        ▼              ▼               ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  University  │ │    Ghost     │ │    Admin     │ │   Mini App   │
│     API      │ │     API      │ │    Panel     │ │  (mini-app/) │
│(university-  │ │ (ghost-app/) │ │(admin-panel/)│ │              │
│   app/)      │ │              │ │              │ │              │
└──────┬───────┘ └──────┬───────┘ └──────────────┘ └──────────────┘
       │                │
       └────────┬───────┘
                │
                ▼
         ┌──────────────┐
         │  PostgreSQL  │
         │   Database   │
         └──────────────┘
```

## 🌐 MAX API (app/)

**Назначение:** Центральный API сервис, который координирует работу всех остальных компонентов.

**Технологии:** Python 3.10+, FastAPI, SQLAlchemy, PostgreSQL

**Порт:** 8003 (по умолчанию)

**База данных:** `maxbot_db`

### Основные функции

1. **Управление университетами**
   - CRUD операции для университетов
   - Multi-tenancy (изоляция данных по `university_id`)
   - Аутентификация через JWT

2. **Управление пользователями**
   - Регистрация и управление пользователями
   - Привязка студентов к университетам

3. **Конфигурация University API**
   - Настройка `base_url` для каждого университета
   - Настройка endpoints для различных функций
   - Хранение конфигурации в БД

4. **Проксирование запросов**
   - Проксирование запросов к University API
   - Автоматическое переключение на Ghost API при отсутствии endpoint
   - Fallback логика (domain → host:port)

5. **Управление данными студентов**
   - Получение расписания
   - Получение списка преподавателей
   - Получение личных данных
   - Получение контактов и платформ

### API Endpoints

- `/api/v1/health` - Health check
- `/api/v1/universities/` - Управление университетами
- `/api/v1/universities/login` - Аутентификация
- `/api/v1/users/` - Управление пользователями
- `/api/v1/config/` - Конфигурация (требует аутентификации)
- `/api/v1/proxy/` - Проксирование запросов

### Зависимости

- PostgreSQL (должна быть запущена)
- University API или Ghost API (для получения данных)

### Конфигурация

```env
DATABASE_URL=postgresql://maxbot:password@postgres:5432/maxbot_db
MAX_API_HOST=0.0.0.0
MAX_API_PORT=8003
MAX_API_DOMAIN_URL=https://api.example.com
```

---

## 🤖 Bot (bot/)

**Назначение:** MAX бот, который взаимодействует с пользователями через MAX Bot API.

**Технологии:** Go 1.23+, MAX Bot API Client

**Зависимости:** MAX API (должен быть запущен)

### Основные функции

1. **Обработка сообщений**
   - Получение сообщений от пользователей
   - Отправка ответов через MAX Bot API

2. **Навигация**
   - Главное меню
   - Меню сервисов
   - Меню профиля

3. **Отображение данных**
   - Расписание занятий
   - Список преподавателей
   - Новости
   - Профиль студента

4. **Интеграция с MAX API**
   - Запросы к MAX API для получения данных
   - Обработка ответов и отображение пользователю

### Структура

- **Handlers** - обработчики событий
- **Keyboards** - компоненты клавиатур
- **Pages** - страницы интерфейса
- **Services** - сервисы для работы с API

### Конфигурация

```env
MAX_BOT_TOKEN=your_bot_token_here
```

**Где получить токен:** [MasterBot](https://max.ru/MasterBot)

---

## 🎛️ Admin Panel (admin-panel/)

**Назначение:** Веб-интерфейс для настройки конфигурации университетов.

**Технологии:** Flutter Web, Dart, Nginx

**Порт:** 8081 (по умолчанию)

**Зависимости:** MAX API (должен быть запущен)

### Основные функции

1. **Аутентификация**
   - Логин администраторов университетов
   - JWT токены для доступа

2. **Настройка конфигурации**
   - Настройка `base_url` для University API
   - Настройка endpoints для различных функций
   - Автоматическое определение endpoints через OpenAPI

3. **Ручной ввод данных**
   - Ввод данных преподавателей
   - Ввод данных расписания
   - Загрузка CSV файлов в Ghost API

4. **Управление модулями**
   - Включение/отключение функций
   - Просмотр статуса endpoints

### Особенности

- **Динамическая конфигурация:** `config.js` генерируется из `.env` при запуске
- **Fallback логика:** Автоматическое переключение на host:port при недоступности domain
- **CSV загрузка:** Загрузка данных в Ghost API для функций без endpoint

### Конфигурация

```env
ADMIN_PANEL_HOST=0.0.0.0
ADMIN_PANEL_PORT=8081
ADMIN_PANEL_DOMAIN_URL=https://admin.example.com
MAX_API_DOMAIN_URL=https://api.example.com
MAX_API_HOST=0.0.0.0
MAX_API_PORT=8003
```

---

## 🏫 University API (university-app/)

**Назначение:** API для взаимодействия с сайтом университета через web scraping.

**Технологии:** Python 3.10+, FastAPI, BeautifulSoup, httpx

**Порт:** 8001 (по умолчанию)

**База данных:** `university_db` (отдельная БД)

### Основные функции

1. **Логин студентов**
   - Выполнение логина на сайте университета
   - Сохранение cookies сессии в БД
   - Управление сессиями

2. **Получение данных**
   - Личные данные студента
   - Расписание занятий
   - Список преподавателей
   - Информация о преподавателях
   - Контакты деканатов и кафедр
   - Список платформ

3. **Web Scraping**
   - Парсинг HTML с сайта университета
   - Обработка AJAX запросов
   - Кэширование данных

### API Endpoints

- `/students/login` - Логин студента
- `/students/personal_data` - Личные данные
- `/students/schedule` - Расписание
- `/students/teachers` - Список преподавателей
- `/students/teacher_info` - Информация о преподавателе
- `/students/contacts` - Контакты
- `/students/platforms` - Платформы

### Особенности

- **Управление сессиями:** Cookies сохраняются в БД для каждого студента
- **Кэширование:** Данные кэшируются для уменьшения нагрузки
- **Multi-tenancy:** Поддержка нескольких университетов

### Тестовый аккаунт

Для тестирования и отладки доступен специальный тестовый аккаунт:

- **Email:** `test@test.ru`
- **Пароль:** `test`

**Важные особенности:**
- Тестовый аккаунт **не привязывается к одному конкретному user_id** мессенджера MAX
- Может использоваться любым пользователем для тестирования
- При логине с `test@test.ru`/`test` система автоматически использует реальные учетные данные студента ЧГУ для входа на сайт университета
- Все данные (расписание, преподаватели, личные данные) загружаются с реального аккаунта студента ЧГУ
- Cookies сохраняются под `test@test.ru`, что позволяет использовать этот email в других запросах

**Использование:**
```bash
# Логин через MAX API
POST /api/v1/proxy/students/login
{
  "user_id": 123456789,
  "university_id": 1,
  "student_email": "test@test.ru",
  "password": "test"
}
```

**Примечание:** Тестовый аккаунт предназначен только для разработки и тестирования. В продакшене рекомендуется использовать реальные учетные данные студентов.

### Конфигурация

```env
UNIVERSITY_API_HOST=0.0.0.0
UNIVERSITY_API_PORT=8001
UNIVERSITY_API_DOMAIN_URL=https://university-api.example.com
UNIVERSITY_DATABASE_URL=postgresql://maxbot:password@postgres:5432/university_db
```

**Важно:** MAX API использует `base_url` из БД (настраивается через админ-панель), а не эти переменные напрямую.

---

## 👻 Ghost API (ghost-app/)

**Назначение:** Резервный API, который симулирует University API, но использует данные из БД вместо scraping.

**Технологии:** Python 3.10+, FastAPI, SQLAlchemy

**Порт:** 8004 (по умолчанию)

**База данных:** `ghost_db` (отдельная БД)

### Основные функции

1. **Симуляция University API**
   - Те же endpoints, что и University API
   - Та же структура ответов
   - Прозрачная замена для MAX API

2. **Хранение данных**
   - Данные хранятся в БД
   - Поддержка всех типов данных (преподаватели, расписание и т.д.)

3. **Загрузка данных**
   - Прием CSV файлов через `/upload`
   - Парсинг CSV и сохранение в БД
   - Поддержка различных форматов данных

### Когда используется

Ghost API активируется автоматически, если:
- Функционал включен в админ-панели
- Endpoint не настроен (пустой)
- Администратор ввел данные вручную и загрузил CSV

### API Endpoints

- `/students/teachers` - Список преподавателей
- `/students/schedule` - Расписание
- `/students/personal_data` - Личные данные
- `/students/contacts` - Контакты
- `/students/platforms` - Платформы
- `/upload` - Загрузка CSV файлов

### Конфигурация

```env
GHOST_API_HOST=0.0.0.0
GHOST_API_PORT=8004
GHOST_API_DOMAIN_URL=https://ghost-api.example.com
GHOST_DATABASE_URL=postgresql://maxbot:password@postgres:5432/ghost_db
```

**Важно:** MAX API использует эти переменные для fallback подключения к Ghost API.

---

## 📱 Mini App (mini-app/)

**Назначение:** Веб-приложение для расширенного функционала бота (открывается внутри MAX).

**Технологии:** React, TypeScript, Vite

**Порт:** 8002 (по умолчанию)

### Основные функции

1. **Расширенный интерфейс**
   - Более богатый UI, чем в боте
   - Интерактивные компоненты
   - Графики и визуализации

2. **Интеграция с MAX**
   - Использование MAX WebApp API
   - Доступ к данным пользователя
   - Интеграция с ботом

3. **Взаимодействие с MAX API**
   - Запросы к MAX API
   - Отображение данных
   - Обработка ошибок

### Конфигурация

```env
MINI_APP_HOST=0.0.0.0
MINI_APP_PORT=8002
MINI_APP_DOMAIN_URL=https://mini-app.example.com
```

---

## 🗄️ PostgreSQL

**Назначение:** База данных для всех сервисов.

**Версия:** PostgreSQL 15

**Порт:** 5432

### Базы данных

1. **maxbot_db** - Основная БД для MAX API
   - Университеты
   - Пользователи
   - Конфигурация
   - Студенты

2. **university_db** - БД для University API
   - Сессии студентов (cookies)
   - Кэшированные данные

3. **ghost_db** - БД для Ghost API
   - Данные преподавателей
   - Данные расписания
   - Другие данные из CSV

### Конфигурация

```env
POSTGRES_USER=maxbot
POSTGRES_PASSWORD=password
POSTGRES_DB=maxbot_db
DATABASE_URL=postgresql://maxbot:password@postgres:5432/maxbot_db
UNIVERSITY_DATABASE_URL=postgresql://maxbot:password@postgres:5432/university_db
GHOST_DATABASE_URL=postgresql://maxbot:password@postgres:5432/ghost_db
```

---

## 🔄 Взаимодействие сервисов

### Поток запроса от пользователя

1. **Пользователь** отправляет сообщение в MAX
2. **Bot** получает сообщение через MAX Bot API
3. **Bot** отправляет запрос к **MAX API**
4. **MAX API** проверяет конфигурацию:
   - Если endpoint настроен → запрос к **University API**
   - Если endpoint пустой → запрос к **Ghost API**
5. **University API** или **Ghost API** возвращает данные
6. **MAX API** возвращает данные **Bot**
7. **Bot** отображает данные пользователю

### Поток настройки конфигурации

1. **Администратор** открывает **Admin Panel**
2. **Admin Panel** подключается к **MAX API** (аутентификация)
3. **Администратор** настраивает `base_url` и endpoints
4. **Admin Panel** отправляет конфигурацию в **MAX API**
5. **MAX API** сохраняет конфигурацию в БД
6. При запросах **MAX API** использует сохраненную конфигурацию

### Поток загрузки данных в Ghost API

1. **Администратор** открывает **Admin Panel**
2. **Администратор** вводит данные вручную (преподаватели, расписание)
3. **Admin Panel** собирает данные в CSV
4. **Admin Panel** отправляет CSV в **Ghost API** (`/upload`)
5. **Ghost API** парсит CSV и сохраняет в БД
6. При запросах **MAX API** получает данные из **Ghost API**

---

## 📊 Сравнение сервисов

| Сервис | Источник данных | Когда используется | База данных |
|--------|----------------|-------------------|-------------|
| **University API** | Web scraping | Endpoint настроен | `university_db` |
| **Ghost API** | БД (CSV) | Endpoint пустой | `ghost_db` |
| **MAX API** | Проксирование | Всегда | `maxbot_db` |


