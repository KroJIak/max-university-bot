# Локальный запуск (без CloudPub)

Руководство по запуску проекта без CloudPub сервисов.

## 📋 Содержание

- [Обзор](#обзор)
- [Локальный запуск (без CloudPub)](#локальный-запуск-без-cloudpub)
- [Управление сервисами](#управление-сервисами)
- [Решение проблем](#решение-проблем)

## 🔍 Обзор

**Локальный запуск** означает запуск проекта **без CloudPub** (все сервисы в Docker, но без CloudPub туннелей).

Этот вариант подходит, если:
- Вам не нужна публикация в облако
- Вы хотите сэкономить ресурсы
- Вы работаете в локальной сети

## 🚀 Локальный запуск (без CloudPub)

Если вам не нужна публикация в облако через CloudPub, используйте файл `docker-compose-without-cloudpub.yml`:

```bash
docker compose -f docker-compose-without-cloudpub.yml up -d
```

**Преимущества:**
- Меньше контейнеров
- Меньше потребление ресурсов
- Быстрее запуск
- Все сервисы работают локально

**Недостатки:**
- Нет публикации в облако
- Нужен прямой доступ к серверам

**⚠️ Важное ограничение:**
- **Mini App будет недоступен**, так как он привязан к домену `max-miniapp.cloudpub.ru` через CloudPub
- Для работы Mini App необходимо использовать стандартный запуск с CloudPub (`docker-compose.yml` или `docker-compose-arm64.yml`)
- Все остальные сервисы (API, Admin Panel, University API, Ghost API) будут работать нормально

Подробнее см. [Docker Compose руководство](docker-compose-guide.md#запуск-без-cloudpub).

## 🔧 Управление сервисами

### Запуск всех сервисов

```bash
docker compose -f docker-compose-without-cloudpub.yml up -d
```

### Остановка всех сервисов

```bash
docker compose -f docker-compose-without-cloudpub.yml down
```

### Перезапуск сервиса

```bash
# Перезапуск конкретного сервиса
docker compose -f docker-compose-without-cloudpub.yml restart api

# Перезапуск всех сервисов
docker compose -f docker-compose-without-cloudpub.yml restart
```

### Просмотр логов

```bash
# Все сервисы
docker compose -f docker-compose-without-cloudpub.yml logs -f

# Конкретный сервис
docker compose -f docker-compose-without-cloudpub.yml logs -f api
docker compose -f docker-compose-without-cloudpub.yml logs -f bot
docker compose -f docker-compose-without-cloudpub.yml logs -f admin-panel
```

### Пересборка образов

```bash
# Пересборка всех образов
docker compose -f docker-compose-without-cloudpub.yml build

# Пересборка конкретного сервиса
docker compose -f docker-compose-without-cloudpub.yml build admin-panel

# Пересборка и запуск
docker compose -f docker-compose-without-cloudpub.yml up -d --build
```

## 🐛 Решение проблем

### Проблема: Порты заняты

**Ошибка:**
```
Error: bind: address already in use
```

**Решение:**
1. Измените порты в `.env`:
   ```env
   MAX_API_PORT=8004
   ADMIN_PANEL_PORT=8082
   ```

2. Или остановите приложение, занимающее порт:
   ```bash
   # Найти процесс на порту
   sudo lsof -i :8003
   
   # Остановить процесс
   sudo kill -9 <PID>
   ```

### Проблема: Ошибки базы данных

**Ошибка:**
```
FATAL: database "maxbot_db" does not exist
```

**Решение:**
1. Проверьте `DATABASE_URL` в `.env`
2. Убедитесь, что PostgreSQL запущен:
   ```bash
   docker compose ps postgres
   ```
3. Проверьте логи:
   ```bash
   docker compose logs postgres
   ```

### Проблема: Сервис не запускается

**Решение:**
1. Проверьте логи:
   ```bash
   docker compose logs -f <service-name>
   ```

2. Проверьте зависимости:
   ```bash
   docker compose ps
   ```

3. Перезапустите сервис:
   ```bash
   docker compose restart <service-name>
   ```

## 📊 Проверка статуса

### Проверка статуса

```bash
# Статус всех сервисов
docker compose -f docker-compose-without-cloudpub.yml ps

# Статус конкретного сервиса
docker compose -f docker-compose-without-cloudpub.yml ps api
```

### Использование ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Использование ресурсов конкретным контейнером
docker stats maxbot_api
```

### Проверка здоровья

```bash
# Health check для postgres
docker compose exec postgres pg_isready -U maxbot

# Проверка API
curl http://localhost:8003/health
```

## 🔐 Безопасность

### Рекомендации

1. **Измените пароли по умолчанию:**
   ```env
   POSTGRES_PASSWORD=very_secure_password
   ```

2. **Ограничьте доступ к портам:**
   - Используйте firewall
   - Настройте reverse proxy (Nginx, Traefik)

3. **Используйте HTTPS:**
   - Настройте SSL сертификаты
   - Используйте доменные URL с HTTPS

4. **Регулярно обновляйте образы:**
   ```bash
   docker compose pull
   docker compose up -d
   ```

## 📝 Полезные команды

### Очистка

```bash
# Удаление неиспользуемых образов
docker image prune -a

# Удаление неиспользуемых volumes
docker volume prune

# Полная очистка (⚠️ удалит все!)
docker system prune -a --volumes
```

### Бэкап БД

```bash
# Создание бэкапа
docker compose exec postgres pg_dump -U maxbot maxbot_db > backup.sql

# Восстановление
docker compose exec -T postgres psql -U maxbot maxbot_db < backup.sql
```

### Просмотр конфигурации

```bash
# Просмотр конфигурации сервиса
docker compose config

# Просмотр конфигурации конкретного сервиса
docker compose config api
```

