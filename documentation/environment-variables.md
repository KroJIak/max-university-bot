# Переменные окружения

Полное описание всех переменных окружения, используемых в проекте Max University Bot.

## 📋 Содержание

- [Обзор](#обзор)
- [Обязательные переменные](#обязательные-переменные)
- [MAX Bot](#max-bot)
- [MAX API](#max-api)
- [Admin Panel](#admin-panel)
- [University API](#university-api)
- [Ghost API](#ghost-api)
- [Mini App](#mini-app)
- [База данных](#база-данных)
- [CloudPub](#cloudpub)
- [Примеры конфигурации](#примеры-конфигурации)

## 🔍 Обзор

Все переменные окружения хранятся в файле `.env` в корне проекта. Файл `.env.example` содержит примеры всех переменных с описаниями.

**Важно:** Файл `.env` не должен коммититься в Git (уже добавлен в `.gitignore`).

## ⚠️ Обязательные переменные

Минимальный набор переменных для запуска проекта:

```env
# Токен MAX бота (обязательно)
MAX_BOT_TOKEN=your_bot_token_here

# Пароль для базы данных (обязательно)
POSTGRES_PASSWORD=your_secure_password

# Токен CloudPub (обязательно, если используется CloudPub)
CLOUDPUB_TOKEN=your_cloudpub_token_here
```

---

## 🤖 MAX Bot

### `MAX_BOT_TOKEN`

**Описание:** Токен MAX бота, полученный от @MasterBot в MAX.

**Тип:** Строка

**Обязательно:** Да

**Пример:**
```env
MAX_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Где получить:**
1. Откройте диалог с [MasterBot](https://max.ru/MasterBot)
2. Создайте нового бота или получите токен существующего
3. Скопируйте токен в `.env`

**Используется в:**
- `bot/` - для подключения к MAX Bot API

---

## 🌐 MAX API

### `MAX_API_HOST`

**Описание:** Хост, на котором будет слушать MAX API.

**Тип:** IP адрес или hostname

**По умолчанию:** `0.0.0.0`

**Пример:**
```env
MAX_API_HOST=0.0.0.0
```

**Примечание:** `0.0.0.0` означает прослушивание на всех интерфейсах.

---

### `MAX_API_PORT`

**Описание:** Порт, на котором будет работать MAX API.

**Тип:** Число (1-65535)

**По умолчанию:** `8003`

**Пример:**
```env
MAX_API_PORT=8003
```

**Важно:** Убедитесь, что порт не занят другим приложением.

---

### `MAX_API_DOMAIN_URL`

**Описание:** Доменный URL для доступа к MAX API (приоритет над HOST:PORT).

**Тип:** URL (с протоколом http:// или https://)

**По умолчанию:** Пусто (не используется)

**Пример:**
```env
MAX_API_DOMAIN_URL=https://api.example.com
```

**Использование:**
- Если указан, используется как primary URL
- Если недоступен, используется fallback на `http://MAX_API_HOST:MAX_API_PORT`
- Используется в админ-панели для подключения к API

**Примечание:** Не должен заканчиваться на `/`.

---

## 🎛️ Admin Panel

### `ADMIN_PANEL_HOST`

**Описание:** Хост, на котором будет слушать админ-панель.

**Тип:** IP адрес или hostname

**По умолчанию:** `0.0.0.0`

**Пример:**
```env
ADMIN_PANEL_HOST=0.0.0.0
```

---

### `ADMIN_PANEL_PORT`

**Описание:** Порт, на котором будет работать админ-панель.

**Тип:** Число (1-65535)

**По умолчанию:** `8081`

**Пример:**
```env
ADMIN_PANEL_PORT=8081
```

**Доступ:** http://localhost:8081 (или по домену, если настроен)

---

### `ADMIN_PANEL_DOMAIN_URL`

**Описание:** Доменный URL для доступа к админ-панели.

**Тип:** URL (с протоколом http:// или https://)

**По умолчанию:** Пусто

**Пример:**
```env
ADMIN_PANEL_DOMAIN_URL=https://admin.example.com
```

**Использование:**
- Используется для генерации `config.js` в админ-панели
- Если не указан, используется `http://ADMIN_PANEL_HOST:ADMIN_PANEL_PORT`

---

### `MAX_API_DOMAIN_URL` (для админ-панели)

**Описание:** Доменный URL MAX API для подключения из админ-панели.

**Тип:** URL

**По умолчанию:** Пусто

**Пример:**
```env
MAX_API_DOMAIN_URL=https://api.example.com
```

**Использование:**
- Админ-панель использует этот URL для подключения к MAX API
- Если не указан, используется fallback на `http://MAX_API_HOST:MAX_API_PORT`

---

### `MAX_API_HOST` и `MAX_API_PORT` (для админ-панели)

**Описание:** Fallback параметры для подключения к MAX API.

**Тип:** IP адрес и порт

**По умолчанию:** `0.0.0.0` и `8003`

**Использование:**
- Используются, если `MAX_API_DOMAIN_URL` не указан или недоступен
- Админ-панель автоматически переключается на fallback при недоступности домена

---

## 🏫 University API

### `UNIVERSITY_API_HOST`

**Описание:** Хост, на котором будет слушать University API.

**Тип:** IP адрес или hostname

**По умолчанию:** `0.0.0.0`

**Пример:**
```env
UNIVERSITY_API_HOST=0.0.0.0
```

**Важно:** Эти переменные используются **только самим university-api сервисом**. MAX API и Admin Panel используют `base_url` из БД (настраивается через админ-панель).

---

### `UNIVERSITY_API_PORT`

**Описание:** Порт, на котором будет работать University API.

**Тип:** Число (1-65535)

**По умолчанию:** `8001`

**Пример:**
```env
UNIVERSITY_API_PORT=8001
```

---

### `UNIVERSITY_API_DOMAIN_URL`

**Описание:** Доменный URL для доступа к University API.

**Тип:** URL

**По умолчанию:** Пусто

**Пример:**
```env
UNIVERSITY_API_DOMAIN_URL=https://university-api.example.com
```

**Использование:**
- Используется только самим university-api сервисом
- MAX API использует `base_url` из БД, который настраивается через админ-панель

---

## 👻 Ghost API

### `GHOST_API_HOST`

**Описание:** Хост, на котором будет слушать Ghost API.

**Тип:** IP адрес или hostname

**По умолчанию:** `0.0.0.0`

**Пример:**
```env
GHOST_API_HOST=0.0.0.0
```

**Важно:** Эти переменные используются **только самим ghost-api сервисом**. MAX API использует эти параметры для fallback подключения к Ghost API.

---

### `GHOST_API_PORT`

**Описание:** Порт, на котором будет работать Ghost API.

**Тип:** Число (1-65535)

**По умолчанию:** `8004`

**Пример:**
```env
GHOST_API_PORT=8004
```

---

### `GHOST_API_DOMAIN_URL`

**Описание:** Доменный URL для доступа к Ghost API.

**Тип:** URL

**По умолчанию:** Пусто

**Пример:**
```env
GHOST_API_DOMAIN_URL=https://ghost-api.example.com
```

**Использование:**
- MAX API использует этот URL как primary для подключения к Ghost API
- Если недоступен, используется fallback на `http://GHOST_API_HOST:GHOST_API_PORT`

---

## 📱 Mini App

### `MINI_APP_HOST`

**Описание:** Хост, на котором будет слушать Mini App.

**Тип:** IP адрес или hostname

**По умолчанию:** `0.0.0.0`

**Пример:**
```env
MINI_APP_HOST=0.0.0.0
```

---

### `MINI_APP_PORT`

**Описание:** Порт, на котором будет работать Mini App.

**Тип:** Число (1-65535)

**По умолчанию:** `8002`

**Пример:**
```env
MINI_APP_PORT=8002
```

---

### `MINI_APP_DOMAIN_URL`

**Описание:** Доменный URL для доступа к Mini App.

**Тип:** URL

**По умолчанию:** Пусто

**Пример:**
```env
MINI_APP_DOMAIN_URL=https://mini-app.example.com
```

---

## 🗄️ База данных

### `DATABASE_URL`

**Описание:** URL подключения к основной базе данных (MAX API).

**Тип:** PostgreSQL connection string

**По умолчанию:** `postgresql://maxbot:maxbot123@postgres:5432/maxbot_db`

**Пример:**
```env
DATABASE_URL=postgresql://username:password@host:port/database
```

**Формат:**
```
postgresql://[user[:password]@][host][:port][/database]
```

**Используется в:**
- `app/` - MAX API

---

### `POSTGRES_USER`

**Описание:** Имя пользователя PostgreSQL.

**Тип:** Строка

**По умолчанию:** `maxbot`

**Пример:**
```env
POSTGRES_USER=maxbot
```

**Используется в:**
- `postgres` сервис (Docker Compose)

---

### `POSTGRES_PASSWORD`

**Описание:** Пароль пользователя PostgreSQL.

**Тип:** Строка

**Обязательно:** Да

**По умолчанию:** `maxbot123` (⚠️ **Измените в продакшене!**)

**Пример:**
```env
POSTGRES_PASSWORD=your_secure_password_here
```

**Важно:** Используйте надежный пароль в продакшене!

---

### `POSTGRES_DB`

**Описание:** Имя основной базы данных.

**Тип:** Строка

**По умолчанию:** `maxbot_db`

**Пример:**
```env
POSTGRES_DB=maxbot_db
```

---

### `UNIVERSITY_DATABASE_URL`

**Описание:** URL подключения к базе данных University API.

**Тип:** PostgreSQL connection string

**По умолчанию:** `postgresql://maxbot:maxbot123@postgres:5432/university_db`

**Пример:**
```env
UNIVERSITY_DATABASE_URL=postgresql://maxbot:password@postgres:5432/university_db
```

**Используется в:**
- `university-app/` - University API

---

### `GHOST_DATABASE_URL`

**Описание:** URL подключения к базе данных Ghost API.

**Тип:** PostgreSQL connection string

**По умолчанию:** `postgresql://maxbot:maxbot123@postgres:5432/ghost_db`

**Пример:**
```env
GHOST_DATABASE_URL=postgresql://maxbot:password@postgres:5432/ghost_db
```

**Используется в:**
- `ghost-app/` - Ghost API

---

## ☁️ CloudPub

### `CLOUDPUB_TOKEN`

**Описание:** Токен для доступа к CloudPub сервису.

**Тип:** Строка

**Обязательно:** Да (если используется CloudPub)

**Пример:**
```env
CLOUDPUB_TOKEN=your_cloudpub_token_here
```

**Что такое CloudPub:**
CloudPub — это сервис для создания туннелей к локальным сервисам, аналогичный ngrok. Он позволяет:
- Публиковать локальные сервисы в интернет через безопасные туннели
- Получать постоянные домены для ваших сервисов (например, `max-miniapp.cloudpub.ru`)
- Обеспечивать доступ к сервисам извне без необходимости настройки портов и файрволов

**Почему мы используем CloudPub:**
1. **Mini App требует публичного доступа:** Mini App должен быть доступен из интернета, так как он открывается внутри MAX. CloudPub предоставляет постоянный домен для Mini App.
2. **Упрощение настройки:** Не требуется настройка DNS, SSL сертификатов и файрволов для каждого сервиса.
3. **Безопасность:** Туннели CloudPub обеспечивают безопасное соединение между локальными сервисами и интернетом.
4. **Гибкость:** Легко переключаться между локальной разработкой и продакшеном.

**Использование:**
- Используется для создания туннелей к локальным сервисам
- Позволяет публиковать сервисы в облако
- Обеспечивает доступ к сервисам из интернета через постоянные домены

**Где получить:**
- Зарегистрируйтесь на платформе CloudPub
- Получите токен в личном кабинете

**Важно:** Без CloudPub Mini App будет недоступен, так как он привязан к домену `max-miniapp.cloudpub.ru`. Для полной функциональности системы рекомендуется использовать CloudPub.

---

## 📝 Примеры конфигурации

### Минимальная конфигурация (локальная разработка)

```env
# Обязательные
MAX_BOT_TOKEN=your_bot_token_here
POSTGRES_PASSWORD=secure_password

# Остальное использует значения по умолчанию
```

### Продакшен конфигурация (с доменами)

```env
# Bot
MAX_BOT_TOKEN=your_bot_token_here

# MAX API
MAX_API_HOST=0.0.0.0
MAX_API_PORT=8003
MAX_API_DOMAIN_URL=https://api.example.com

# Admin Panel
ADMIN_PANEL_HOST=0.0.0.0
ADMIN_PANEL_PORT=8081
ADMIN_PANEL_DOMAIN_URL=https://admin.example.com

# University API
UNIVERSITY_API_HOST=0.0.0.0
UNIVERSITY_API_PORT=8001
UNIVERSITY_API_DOMAIN_URL=https://university-api.example.com

# Ghost API
GHOST_API_HOST=0.0.0.0
GHOST_API_PORT=8004
GHOST_API_DOMAIN_URL=https://ghost-api.example.com

# Mini App
MINI_APP_HOST=0.0.0.0
MINI_APP_PORT=8002
MINI_APP_DOMAIN_URL=https://mini-app.example.com

# Database
POSTGRES_USER=maxbot
POSTGRES_PASSWORD=very_secure_password_here
POSTGRES_DB=maxbot_db
DATABASE_URL=postgresql://maxbot:very_secure_password_here@postgres:5432/maxbot_db
UNIVERSITY_DATABASE_URL=postgresql://maxbot:very_secure_password_here@postgres:5432/university_db
GHOST_DATABASE_URL=postgresql://maxbot:very_secure_password_here@postgres:5432/ghost_db

# CloudPub
CLOUDPUB_TOKEN=your_cloudpub_token_here
```

### Конфигурация без CloudPub

```env
# Все то же самое, но без CLOUDPUB_TOKEN
# И используйте docker-compose-without-cloudpub.yml
```

---

## 🔄 Fallback логика

### MAX API → University API / Ghost API

1. **Primary:** Используется `base_url` из БД (настраивается в админ-панели)
2. **Fallback:** Если `base_url` - это домен, но он недоступен, используется `http://UNIVERSITY_API_HOST:UNIVERSITY_API_PORT`

### MAX API → Ghost API

1. **Primary:** `GHOST_API_DOMAIN_URL` (если указан)
2. **Fallback:** `http://GHOST_API_HOST:GHOST_API_PORT`

### Admin Panel → MAX API

1. **Primary:** `MAX_API_DOMAIN_URL` (если указан)
2. **Fallback:** `http://MAX_API_HOST:MAX_API_PORT`

---

## ⚠️ Важные замечания

1. **Безопасность:**
   - Никогда не коммитьте `.env` в Git
   - Используйте надежные пароли в продакшене
   - Ограничьте доступ к файлу `.env` (права 600)

2. **Порты:**
   - Убедитесь, что порты не заняты другими приложениями
   - В Docker Compose порты пробрасываются на хост

3. **Домены:**
   - Доменные URL должны быть доступны из контейнеров
   - Используйте HTTPS в продакшене
   - Не добавляйте `/` в конце URL

4. **База данных:**
   - Каждый сервис может использовать отдельную БД
   - Или все могут использовать одну БД с разными схемами
   - Убедитесь, что пользователь имеет права на создание БД


