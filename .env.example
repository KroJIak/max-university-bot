# MAX Bot Configuration (Go)
# Получить можно в диалоге с MasterBot: https://max.ru/MasterBot
MAX_BOT_TOKEN=your_bot_token_here


# MAX API Configuration (Main API)
MAX_API_HOST=0.0.0.0
MAX_API_PORT=8003
MAX_API_DOMAIN_URL=https://max-api.cloudpub.ru
# Доменный URL для доступа к MAX API (приоритет над HOST:PORT)

# Admin Panel Configuration
ADMIN_PANEL_HOST=0.0.0.0
ADMIN_PANEL_PORT=8081
ADMIN_PANEL_DOMAIN_URL=https://max-admin-panel.cloudpub.ru
# Доменный URL для доступа к Admin Panel (приоритет над HOST:PORT)

# University API Configuration
UNIVERSITY_API_HOST=0.0.0.0
UNIVERSITY_API_PORT=8001
UNIVERSITY_API_DOMAIN_URL=https://csu-university-api.cloudpub.ru
# Доменный URL для доступа к University API (приоритет над HOST:PORT)
# Используется MAX API для проксирования запросов к University API

# Mini App Configuration
MINI_APP_HOST=0.0.0.0
MINI_APP_PORT=8002
MINI_APP_DOMAIN_URL=https://max-miniapp.cloudpub.ru
# Доменный URL для доступа к Mini App (приоритет над HOST:PORT)

# Ghost API Configuration
# ВАЖНО: Эти переменные используются ТОЛЬКО самим ghost-api сервисом
# MAX API и Admin Panel НЕ должны знать эти параметры
# MAX API использует base_url из БД (настраивается через Admin Panel)
GHOST_API_HOST=0.0.0.0
GHOST_API_PORT=8004
GHOST_API_DOMAIN_URL=https://max-ghost-api.cloudpub.ru
# Доменный URL для доступа к Ghost API (приоритет над HOST:PORT)
# Используется только самим ghost-api сервисом

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://maxbot:maxbot123@postgres:5432/maxbot_db
POSTGRES_USER=maxbot
POSTGRES_PASSWORD=maxbot123
POSTGRES_DB=maxbot_db

# University API Database (отдельная БД)
UNIVERSITY_DATABASE_URL=postgresql://maxbot:maxbot123@postgres:5432/university_db

# Ghost API Database (отдельная БД)
GHOST_DATABASE_URL=postgresql://maxbot:maxbot123@postgres:5432/ghost_db

# CloudPub Configuration
CLOUDPUB_TOKEN=dpiaF-CPEc715LNkLUl8Ga5EsfqLIP6Zt8_rXYxbmDE