# MAX Bot API

Основной API сервис для управления ботом университета в системе MAX. Предоставляет REST API для управления университетами, пользователями, студентами и их данными.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Установка и запуск](#установка-и-запуск)
- [API Endpoints](#api-endpoints)
- [Аутентификация](#аутентификация)
- [Конфигурация](#конфигурация)
- [Разработка](#разработка)

## 🎯 Описание

**MAX Bot API** — это FastAPI приложение, которое служит центральным API для управления ботами университетов. Сервис поддерживает multi-tenancy архитектуру, позволяя нескольким университетам использовать одну систему с изолированными данными.

### Основные функции:

- 🔐 **Multi-tenancy**: Поддержка множества университетов с изолированными данными
- 🔑 **JWT аутентификация**: Безопасный доступ к защищенным endpoints
- 🎛️ **Гибкая конфигурация**: Настройка endpoints для каждого университета
- 📊 **Управление данными**: CRUD операции для университетов, пользователей и студентов
- 🔄 **Проксирование запросов**: Интеграция с внешними API университетов
- 🗺️ **Карты корпусов**: Получение информации о корпусах с координатами

## 🚀 Возможности

### Управление университетами
- Создание, получение, обновление и удаление университетов
- Аутентификация через логин/пароль
- Получение JWT токенов для доступа к защищенным endpoints

### Управление пользователями
- CRUD операции для пользователей системы MAX
- Привязка пользователей к университетам

### Управление студентами
- Привязка аккаунтов студентов к пользователям
- Получение данных студентов (расписание, преподаватели, контакты и т.д.)
- Проксирование запросов к API университетов

### Конфигурация
- Настройка endpoints для различных функций университета
- Хранение конфигурации для каждого университета отдельно
- Защищенный доступ через JWT токены

## 🏗️ Архитектура

Проект следует принципам чистой архитектуры с разделением на слои:

```
app/
├── api/                    # API endpoints и роутеры
│   └── v1/
│       ├── health.py      # Health check endpoint
│       ├── universities.py # Управление университетами
│       ├── users.py       # Управление пользователями
│       ├── config.py      # Конфигурация университетов
│       ├── proxy.py       # Проксирование запросов
│       ├── schemas/       # Pydantic схемы для валидации
│       └── services/      # Бизнес-логика
├── core/                   # Ядро приложения
│   ├── database.py        # Настройка БД (SQLAlchemy)
│   ├── auth.py            # JWT аутентификация
│   ├── password.py        # Хеширование паролей
│   ├── encryption.py      # Шифрование данных
│   └── url_helper.py      # Утилиты для работы с URL
├── models/                 # SQLAlchemy модели
│   ├── university.py      # Модель университета
│   ├── users.py           # Модель пользователя
│   ├── student_credentials.py # Учетные данные студентов
│   ├── university_config.py   # Конфигурация университета
│   └── messages.py        # Модель сообщений
├── repositories/           # Слой доступа к данным
│   ├── university_repository.py
│   ├── user_repository.py
│   ├── student_credentials_repository.py
│   ├── university_config_repository.py
│   └── message_repository.py
├── main.py                 # Точка входа приложения
├── requirements.txt        # Зависимости Python
└── Dockerfile             # Docker образ

```

### Технологический стек

- **FastAPI** — веб-фреймворк для создания API
- **SQLAlchemy** — ORM для работы с базой данных
- **PostgreSQL** — реляционная база данных
- **Pydantic** — валидация данных
- **JWT** — аутентификация через токены
- **bcrypt** — хеширование паролей
- **cryptography** — шифрование данных

## 📦 Установка и запуск

### Требования

- Python 3.11+
- PostgreSQL 15+
- Docker (опционально, для контейнеризации)

### Локальная установка

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Настройте переменные окружения:**
   Создайте файл `.env` в корне проекта:
   ```env
   DATABASE_URL=postgresql://maxbot:maxbot123@localhost:5432/maxbot_db
   JWT_SECRET_KEY=your-secret-key-change-in-production
   MAX_API_HOST=0.0.0.0
   MAX_API_PORT=8003
   ```

3. **Создайте базу данных:**
   ```bash
   createdb maxbot_db
   ```

4. **Запустите приложение:**
   ```bash
   uvicorn main:app --host 0.0.0.0 --port 8003 --reload
   ```

### Запуск через Docker

1. **Соберите образ:**
   ```bash
   docker build -t max-bot-api .
   ```

2. **Запустите контейнер:**
   ```bash
   docker run -d \
     --name max-bot-api \
     -p 8003:8003 \
     -e DATABASE_URL=postgresql://maxbot:maxbot123@postgres:5432/maxbot_db \
     -e JWT_SECRET_KEY=your-secret-key \
     max-bot-api
   ```

### Запуск через Docker Compose

Сервис автоматически запускается при использовании `docker-compose.yml` из корня проекта.

## 📚 API Endpoints

### Health Check

```http
GET /api/v1/health
```

Проверяет работоспособность API.

**Ответ:**
```json
{
  "status": "ok",
  "service": "max-bot-api"
}
```

### Университеты

#### Создание университета
```http
POST /api/v1/universities
Content-Type: application/json

{
  "name": "Чувашский государственный университет"
}
```

#### Получение списка университетов
```http
GET /api/v1/universities?limit=10&offset=0
```

#### Получение университета по ID
```http
GET /api/v1/universities/{university_id}
```

#### Обновление университета
```http
PUT /api/v1/universities/{university_id}
Content-Type: application/json

{
  "name": "Новое название",
  "login": "admin",
  "password": "newpassword"
}
```

#### Удаление университета
```http
DELETE /api/v1/universities/{university_id}
```

#### Аутентификация университета
```http
POST /api/v1/universities/login
Content-Type: application/json

{
  "university_id": 1,
  "login": "admin",
  "password": "admin"
}
```

**Ответ:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

### Пользователи

#### Создание пользователя
```http
POST /api/v1/users
Content-Type: application/json

{
  "max_user_id": 12345,
  "university_id": 1
}
```

#### Получение пользователя
```http
GET /api/v1/users/{user_id}
```

#### Получение пользователя по MAX User ID
```http
GET /api/v1/users/by-max-id/{max_user_id}?university_id=1
```

### Конфигурация (требует аутентификации)

#### Получение конфигурации
```http
GET /api/v1/config/{university_id}
Authorization: Bearer <token>
```

#### Обновление конфигурации
```http
PUT /api/v1/config/{university_id}
Authorization: Bearer <token>
Content-Type: application/json

{
  "schedule_endpoint": "https://university.ru/api/schedule",
  "teachers_endpoint": "https://university.ru/api/teachers",
  ...
}
```

### Данные студентов (проксирование)

#### Получение расписания
```http
GET /api/v1/students/{user_id}/schedule?university_id=1
```

#### Получение преподавателей
```http
GET /api/v1/students/{user_id}/teachers?university_id=1
```

#### Получение контактов
```http
GET /api/v1/students/{user_id}/contacts?university_id=1
```

#### Получение карт корпусов
```http
GET /api/v1/students/{user_id}/buildings?university_id=1
```

Полная документация API доступна по адресу: `http://localhost:8003/docs` (Swagger UI) или `http://localhost:8003/redoc` (ReDoc).

## 🔐 Аутентификация

Для доступа к защищенным endpoints (конфигурация) требуется JWT токен.

### Получение токена

1. Выполните запрос на `/api/v1/universities/login` с `university_id`, `login` и `password`
2. Получите `access_token` из ответа
3. Используйте токен в заголовке `Authorization: Bearer <token>`

### Использование токена

```http
GET /api/v1/config/{university_id}
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

Токен действителен в течение 24 часов.

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `DATABASE_URL` | URL подключения к PostgreSQL | `postgresql://maxbot:maxbot123@postgres:5432/maxbot_db` |
| `JWT_SECRET_KEY` | Секретный ключ для JWT токенов | `your-secret-key-change-in-production` |
| `MAX_API_HOST` | Хост для запуска сервера | `0.0.0.0` |
| `MAX_API_PORT` | Порт для запуска сервера | `8003` |

### База данных

При первом запуске приложение автоматически создает необходимые таблицы в базе данных. Также создаются тестовые университеты с логином `admin` и паролем `admin`, если база данных пуста.

## 🛠️ Разработка

### Структура кода

- **API Layer** (`api/v1/`) — обработка HTTP запросов, валидация входных данных
- **Service Layer** (`api/v1/services/`) — бизнес-логика приложения
- **Repository Layer** (`repositories/`) — абстракция доступа к данным
- **Model Layer** (`models/`) — SQLAlchemy модели для работы с БД
- **Core** (`core/`) — базовые утилиты (БД, аутентификация, шифрование)

### Добавление нового endpoint

1. Создайте функцию в соответствующем роутере (`api/v1/`)
2. Добавьте Pydantic схему в `api/v1/schemas/` (если нужна)
3. Используйте репозиторий для доступа к данным
4. Зарегистрируйте роутер в `main.py` (если создаете новый)

### Тестирование

```bash
# Запуск с автоперезагрузкой для разработки
uvicorn main:app --host 0.0.0.0 --port 8003 --reload

# Просмотр логов
docker compose logs -f api
```

### Миграции базы данных

При изменении моделей SQLAlchemy автоматически создает таблицы при запуске. Для продакшена рекомендуется использовать Alembic для миграций.

## 📝 Примечания

- По умолчанию создаются тестовые университеты с логином `admin` и паролем `admin`
- Все пароли хранятся в захешированном виде (bcrypt)
- Учетные данные студентов шифруются перед сохранением в БД
- API поддерживает CORS для работы с фронтендом

## 🔗 Связанные сервисы

- **Bot** — MAX бот (Go)
- **Admin Panel** — веб-админ-панель (Flutter Web)
- **University API** — API для работы с сайтом университета
- **Ghost API** — резервный API для ручных данных
- **Mini App** — веб-приложение для бота

## 📄 Лицензия

MIT

